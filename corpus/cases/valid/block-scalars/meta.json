{
  "summary": "Literal and folded block scalar strings are accepted",
  "tags": ["strings", "block-scalar", "literal", "folded"],
  "spec_refs": ["grammar.string-forms", "grammar.block-scalar-strings"],
  "modes": ["default", "strict", "y11safety"],
  "emit_profiles": {
    "default": "expected_emit_default.yaml",
    "strict": "expected_emit_strict.yaml",
    "roundtrip": "expected_emit_roundtrip.yaml"
  },
  "notes": "Relies on default YAML clip behavior for both block scalar forms."
}
