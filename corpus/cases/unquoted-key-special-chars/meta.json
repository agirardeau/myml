{
  "summary": "Unquoted keys may contain period, slash, dollar, parentheses, and tilde",
  "tags": [
    "unquoted-key",
    "mapping"
  ],
  "spec_refs": [
    "grammar.unquoted-keys"
  ],
  "notes": "Values are integers to keep the case focused on key characters across all modes.",
  "parse_profiles": {
    "baseline": {
      "modes": "all",
      "expect_node_graph": "expected_node_graph.json"
    }
  },
  "emit_profiles": {
    "default": {
      "mode": "default",
      "expect_output": "expected_emit_default.yaml"
    },
    "strict": {
      "mode": "strict",
      "expect_output": "expected_emit_strict.yaml"
    }
  }
}
