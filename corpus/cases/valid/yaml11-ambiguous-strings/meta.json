{
  "summary": "Quoted strings can stay valid while covering YAML 1.1-ambiguous spellings",
  "tags": ["string", "y11safety", "strict", "emit"],
  "spec_refs": ["optional.strict-mode", "optional.yaml11-safety", "serialization-defaults"],
  "modes": ["default", "strict", "y11safety"],
  "emit_profiles": {
    "default": "expected_emit_default.yaml",
    "strict": "expected_emit_strict.yaml",
    "y11safety": "expected_emit_y11safety.yaml",
    "roundtrip": "expected_emit_roundtrip.yaml"
  },
  "notes": "Default mode can emit these values plainly, while strict and y11safety must quote them."
}
