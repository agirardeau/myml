{
  "summary": "Block mapping with nested sequence and nested mappings",
  "tags": ["block", "mapping", "sequence", "nesting"],
  "spec_refs": ["grammar.block-style-mappings", "grammar.block-style-sequences", "containers.block-style", "data-model.mapping-keys-strings"],
  "modes": ["default"],
  "emit_profiles": {
    "default": "expected_emit_default.yaml",
    "strict": "expected_emit_strict.yaml"
  },
  "notes": "Exercises the core block-style container forms."
}
