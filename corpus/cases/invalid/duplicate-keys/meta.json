{
  "summary": "Duplicate mapping keys are rejected",
  "tags": ["duplicate-key", "mapping"],
  "spec_refs": ["containers.duplicate-keys"],
  "modes": ["default"],
  "notes": "Uses the same unquoted key spelling twice."
}
